import React, {Component} from 'react';

class Button extends Component {
    render() {
        return (
            <button>
                My button
            </button>
        );
    }
}

Button.propTypes = {};

export default Button;